# Railway Worker Environment Variables
# Copy these to Railway's environment variables section

# Required: Replicate API for image generation
REPLICATE_API_TOKEN=your_replicate_token_here

# Required: OpenAI API for prompt generation
OPENAI_API_KEY=your_openai_api_key_here

# Required: Vercel Blob storage token
BLOB_READ_WRITE_TOKEN=your_vercel_blob_token_here

# Optional: Vercel Blob base URL (will be auto-set by Vercel)
# VERCEL_BLOB_BASE_URL=https://your-blob-store.vercel-storage.com

# Optional: Run generation on startup (for testing)
RUN_ON_STARTUP=false

# Optional: Timezone for cron schedule (default: UTC)
TZ=America/Los_Angeles

# Optional: Node environment
NODE_ENV=production

# Port (Railway will auto-set this)
PORT=3001
